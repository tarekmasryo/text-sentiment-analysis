# Artifacts

This folder is used for files generated by the notebook.

Expected generated files:

```text
sentiment_pipeline.joblib
threshold.json
metrics.json
training_config.json
model_card.md
reload_smoke_test.py
```

The generated files are ignored by Git and can be recreated by running the notebook.
